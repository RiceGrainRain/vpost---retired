 if (titleController.text.isEmpty) {
                  showErrorMessage("Title cannot be empty");
                } else if (descriptionController.text.isEmpty) {
                  showErrorMessage("Description Cannot be empty");
                } else {
                    if (isLoaded){
                    uploadPost();
                    showSuccessMessage();
                    Timer(Duration(seconds: 1), () {
                      Navigator.pop(context);
                      Navigator.pop(context);
                    });
                    } else {
                        if (infolinkController.text.isEmpty || gclinkController.text.isEmpty){
                            showAlertMessage("Missing some fields! Hit upload to post anyways!")
                            isLoaded = true;
                    } else {
                        uploadPost();
                            showSuccessMessage();
                            Timer(Duration(seconds: 1), () {
                            Navigator.pop(context);
                            Navigator.pop(context);
                            });
                    }
                    }
                }